<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CocktailBar" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/didact_gothic_regular</item>
    </style>

    <style name="Theme.CocktailBar" parent="Base.Theme.CocktailBar" />
    
    <style name="mainText">
        <item name="android:textColor">@color/mainTextColor</item>
    </style>

    <style name="secondText">
        <item name="android:textColor">@color/secondTextColor</item>
    </style>

    <style name="auth_edit_text_back">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/mainTextColor</item>
    </style>
</resources>